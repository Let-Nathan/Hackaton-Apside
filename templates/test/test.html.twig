{% extends 'base.html.twig' %}

{% block body %}
    {{ dump(users.0.technologies) }}
    {% for user in users %}
        {{ user.firstname }}
        {% for technologies in user.technologies %}
        {{ dump(technologies)}}
        {% endfor %}
    {% endfor %}
{% endblock %}